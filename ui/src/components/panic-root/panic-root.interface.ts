export interface PanicRootInterface {

    /**
     * List of supported base chains (blockchain frameworks).
     */
    // type `any` will be replaced once we have our data types rdy for UI Iteration 2
    baseChains: any
}