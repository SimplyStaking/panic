# Command runs the typescript server so time isn't wasted rebuilding it to
# check for any new changes, to install: npm i -g nodemon.
# Incase of "ts-node" exec not found error install it using:
# npm install -g ts-node
nodemon src/server.ts