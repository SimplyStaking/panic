image: node:latest

before_script:
    - cd src/web-installer/frontend

stages:
    - build
    - test

cache:
    paths:
        - src/web-installer/frontend/node_modules/

install:
    stage: build
    script:
        - npm install
    artifacts:
        paths:
        - node_modules/

test:
    stage: test
    script:
        - npm test